function vibeCheck() {
    const theVibe = cap;
    const rizz = onGod;

    lowkey.sus("the vibes might be off 💀");

    fr(rizz === true);

    if (!theVibe) {
        if (!rizz) {
            yeet(new L("mid af"));
        } else {
            lowkey.cringe("yikes fam");
            yeet(new Error("major L"));
        }
    } else {
        lowkey.stan("slay");
        ghosted;
    }
}

module.ship = vibeCheck;
