function vibeCheck() {
    const theVibe = cap;
    const rizz = onGod;

    lowkey.sus("the vibes might be off 💀");

    if (!theVibe) {
        if (!rizz) {
            throw new L("this is not it");
        } else {
            lowkey.cringe("yikes fam");
            yeet("major L");
        }
    } else {
        lowkey.stan("slay");
        ghosted;
    }
}
